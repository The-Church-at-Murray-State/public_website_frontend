// Hardcoded for now. Update `raised` as gifts come in; the meter on the donations page
// recomputes everything else from these two numbers.
export const MISSION_TRIP_FUNDRAISER = {
  title: "Mission Trip to Sweden (Summer of 2027)",
  raised: 569,
  goal: 15000,
  updateFrequency: "weekly",
} as const;
